<!DOCTYPE html>
<html>
   <head>
       <meta charset="UTF-8" />
       <title>{% block title %}Mon titre{% endblock %}</title>


                {% block stylesheets %}
                   <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
                           <script src="{{ asset('js/jquery.min.js') }}"></script>
                           <script src="{{ asset('js/bootstrap.js') }}"></script>
                           <script type="text/javascript" src="script.js"></script>
                {% endblock %}

       <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
   </head>

   <body>
       {% block body %}
       {% endblock %}

                {% block javascripts %}
           <script src="{{ asset('js/bootstrap.js') }}"></script>
                {% endblock %}
   </body>
</html>
{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="margintop panel panel-danger">
            <div class="panel-heading">
                <h3 class="panel-title">{{ jouet.nom }}</h3>
            </div>
            <div class="panel-body">
                <table>
                    <tbody>
                        <tr>
                            <p class="grey">{{ jouet.description }}</p>
                        </tr>
                    </tbody>
                    <div class="container">
                        <div class="row">
                            <img class="thumbnail col-xs-4 col-xs-offset-4" src="{{ jouet.vignette }}">
                        </div>
                    </div>
                </table>
            </div>
        </div>

        <ul>
            <li>
                <a class="link" href="{{ path('jouet_index') }}">Retour à la liste des jouets</a>
            </li>
        </ul>
    </div>
</div>
{% endblock %}
