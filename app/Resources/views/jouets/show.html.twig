{% extends 'base.html.twig' %}

{% block body %}
    <h1>Jouet</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jouet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ jouet.nom }}</td>
            </tr>
            <tr>
                <th>Vignette</th>
                <td>{{ jouet.vignette }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('jouets_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('jouets_edit', { 'id': jouet.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
